# 보이스닥터 블로그 자동화 — 작업 규칙

## 이 저장소는 무엇인가

네이버 블로그 `https://blog.naver.com/voicedr` 의 원고를 **월·화 각 3편** 생성하는 시스템.
주 6편을 만들어 사장님이 골라서 발행한다. 수요일~일요일에는 생성하지 않는다.
브랜드는 **비의료 발성 트레이닝 센터**(17년, 1:1 전문).

## 원고를 만들 때

반드시 `daily-blog` 스킬을 사용한다. 스킬 없이 즉흥 생성하지 않는다.

읽어야 할 순서:
1. `brand/author-profile.md` — **최상위 기준.** "이 사람이라면 이 표현을 쓸까"
   를 판단하는 근거. 한 사람의 일관된 문체를 재현하는 것이 목적이다
2. `brand/guardrails.md` — 법적·유사문서 규칙. 톤보다 우선한다
3. `brand/tone-and-manner.md` **H섹션** — 사장님이 고쳐 발행한 것에서 뽑은 규칙.
   실측값(A~F)보다 우선한다
4. `brand/tone-and-manner.md` A~F — 실측된 말투·구조
5. `corpus/*.md` — 톤의 정답지
6. `brand/profile.md` — CTA 정보

발행본이 들어오면 `scripts/compare_revision.py` 로 초안과 비교한다.
문구는 1편으로 확정하되, **구조·스타일은 3편 전부에서 나타날 때만** 확정한다.
2편 반복은 우연일 수 있다 (실제로 두 번 틀렸다).

**최신 발행본을 따라 매번 기준을 옮기지 않는다.** 그렇게 하면 기준점이
사라지고 이동 평균만 남는다. 프로파일은 세 편 연속 어긋날 때만 고친다.

## 네이버 접근 제한

이 환경은 네이버 도메인이 조직 네트워크 정책으로 차단되어 있다
(`blog.naver.com` `search.naver.com` `openapi.naver.com` 모두 CONNECT 403).
발행 글이나 상위노출 문서를 직접 열어볼 수 없으므로 본문을 받아야 한다.
매 실행마다 접근 가능 여부를 다시 확인하고, 막혀 있으면 그 사실을 보고에 남긴다.
추측으로 트렌드를 지어내지 않는다.

## 절대 규칙

- **유사문서 방지가 최우선.** 생성 후 `python3 scripts/check_similarity.py` 를 반드시 돌린다
- 분량은 **상한만** 지킨다. 2,000자를 넘기지 않는다.
  **하한을 채우려고 문장을 덧붙이지 않는다** — 사장님이 매번 잘라낸 부분이다
- 원고를 다 쓰면 `python3 scripts/wrap_lines.py` 로 줄바꿈 양식을 적용한다
  (한 줄 24자 이하, 문장마다 새 줄. 모바일에서 오른쪽 2~3칸 여백)
- 이모지 금지
- 창작 후기·가짜 사례 금지. 실제 사례가 없으면 문단 자체를 뺀다
- 근거 없는 수치 금지
- `치료` `완치` `처방` `환자` 사용 금지 (비의료 사업자)
- `진단` `교정` 은 브랜드 기존 용법이므로 **유지**한다
- 브랜드명 표기는 **`보이스 닥터`** (띄어쓰기)
- 제목은 **6개 후보**를 유형별로 뽑는다. 메인 키워드는 맨 앞에 1회만
- 줄바꿈 양식이 이 블로그의 정체성이다. `wrap_lines.py` 없이 발행하지 않는다

## 발행 주기

- 월요일, 화요일 아침 06:57 (한국시간)에 각각 3편
- Routine cron은 UTC 기준 `57 21 * * 0,1`
- 발행량을 임의로 늘리지 않는다. 유사문서 위험이 발행량에 비례한다
- 3편은 **고르라고 주는 것**이다. 셋의 성격이 뚜렷이 달라야 고를 이유가 생긴다

## 중단해야 할 때

키워드 큐의 ready 항목이 6개 미만이면 원고를 만들지 말고 사장님께 알린다.
임의로 키워드를 지어내지 않는다.

CTA는 실제 연락처를 넣지 않는 것이 방침이다. `brand/profile.md` 참고.
연락처가 비어 있다고 해서 중단하거나 사장님께 다시 묻지 않는다.

## 전달

`delivery/artifact-url.txt` 의 URL로 **같은 웹페이지를 갱신**한다.
새 URL을 만들지 않는다. 발행 전 반드시 현재 버전을 read 한 뒤 그 위에 갱신한다.
