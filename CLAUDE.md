# 보이스닥터 블로그 자동화 — 작업 규칙

## 이 저장소는 무엇인가

네이버 블로그 `https://blog.naver.com/voicedr` 의 원고를 **월·화 각 2편** 생성하는 시스템.
주 4편이며, 수요일~일요일에는 생성하지 않는다.
브랜드는 **비의료 발성 트레이닝 센터**(17년, 1:1 전문).

## 원고를 만들 때

반드시 `daily-blog` 스킬을 사용한다. 스킬 없이 즉흥 생성하지 않는다.

읽어야 할 순서:
1. `brand/guardrails.md` — 가장 강한 규칙. 톤보다 우선한다
2. `brand/tone-and-manner.md` — 실측된 말투·구조·분량
3. `corpus/*.md` — 톤의 정답지
4. `brand/profile.md` — CTA 정보

## 절대 규칙

- **유사문서 방지가 최우선.** 생성 후 `python3 scripts/check_similarity.py` 를 반드시 돌린다
- 분량은 공백 제외 **1,900~2,400자**. 길게 쓰지 않는다
- 이모지 금지
- 창작 후기·가짜 사례 금지. 실제 사례가 없으면 문단 자체를 뺀다
- 근거 없는 수치 금지
- `치료` `완치` `처방` `환자` 사용 금지 (비의료 사업자)
- `진단` `교정` 은 브랜드 기존 용법이므로 **유지**한다
- 브랜드명 표기는 **`보이스 닥터`** (띄어쓰기)
- 줄바꿈 패턴(1~2문장마다, 단락 사이 빈 줄 3~6개)이 이 블로그의 정체성이다. 붙여 쓰지 않는다

## 발행 주기

- 월요일, 화요일 아침 06:57 (한국시간)에 각각 2편
- Routine cron은 UTC 기준 `57 21 * * 0,1`
- 발행량을 임의로 늘리지 않는다. 유사문서 위험이 발행량에 비례한다

## 중단해야 할 때

키워드 큐의 ready 항목이 4개 미만이거나 `brand/profile.md` CTA가 비어 있으면
원고를 만들지 말고 사장님께 알린다. 임의로 채우지 않는다.

## 전달

`delivery/artifact-url.txt` 의 URL로 **같은 웹페이지를 갱신**한다.
새 URL을 만들지 않는다. 발행 전 반드시 현재 버전을 read 한 뒤 그 위에 갱신한다.
